<span onclick=@(() => HandleClick(Position)) class="board__cell">@(Symbol)</span>

@functions
{
    [Parameter]
    string Symbol { get; set; }
    [Parameter]
    string View { get; set; }
    [Parameter]
    int Position { get; set; }

    public void HandleClick(int position)
    {
        TicTacToe.Core.UI.Instance.Manager.HandleClick(position);
    }
}
