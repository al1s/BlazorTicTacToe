@foreach (var cell in Cells)
{
    <Cell Symbol="@(cell.Symbol)" View="@(cell.View)" Position="@cell.Position"/>
}

@functions
{
    [Parameter]
    TicTacToe.Core.Classes.Cell[] Cells { get; set; }
}
